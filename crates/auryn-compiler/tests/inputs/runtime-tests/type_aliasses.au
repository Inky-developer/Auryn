// expect-output:
// 1

type Scalar = I32
type Coord = {x: Scalar, y: Scalar}

fn main() { 
	let coords: []Coord = std.arrayOfZeros(2)
	coords.set(0, {x: 1, y: 2})
	coords.set(1, {x: 4, y: 5})
	std.print(coords.get(0).x)
}

