// expect-output:
// Foo { value: Hi }

// file: main
type Foo = foo.Foo[String]

fn main() {
	let f = Foo {
		value: "Hi"
	}
	test(f)
}

fn test[T](val: foo.Foo[T]) {
	std.print(val)
}

// file: foo
struct Foo[T] {
	value: T
}
