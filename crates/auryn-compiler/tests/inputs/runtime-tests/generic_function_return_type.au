// expect-output:
// [1, 2]

fn main() {
	std.print(make("1", "2"))
}

fn make[T](first: T, second: T) -> []T {
    let array: []T = std.arrayOfZeros(2)
    array.set(0, first)
    array.set(1, second)
	return array
}
